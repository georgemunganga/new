import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// This creates the root and renders the app
createRoot(document.getElementById("root")!).render(<App />);
